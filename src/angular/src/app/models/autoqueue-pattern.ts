export interface AutoQueuePattern {
  pattern: string;
}

/**
 * AutoQueuePattern as serialized by the backend.
 * Note: naming convention matches that used in JSON.
 */
export interface AutoQueuePatternJson {
  pattern: string;
}
